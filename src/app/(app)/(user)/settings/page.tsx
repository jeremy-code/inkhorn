import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "settings | inkhorn",
};

const SettingsPage = () => {
  return <div>SettingsPage</div>;
};

export default SettingsPage;
