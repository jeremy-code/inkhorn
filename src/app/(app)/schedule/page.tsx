import React from "react";
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "schedule | inkhorn",
};

const SchedulePage = () => {
  return <div>Schedule</div>;
};

export default SchedulePage;
